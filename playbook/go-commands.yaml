# use 'fish -c' to execute go get with go-env variables in config.fish
- name: install go commands
  shell: |
    fish -c 'go install -v github.com/motemen/ghq'
    fish -c 'go install -v github.com/monochromegane/the_platinum_searcher/cmd/pt'
    fish -c 'go install -v github.com/mattn/memo'
