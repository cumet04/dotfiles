#!/bin/bash

set -e

git branch --merged origin/$(git default-branch) | \
  sed 's/^[ *]*//' | \
  xargs --no-run-if-empty git branch -d

selected=$(git branch -vv | sed 's/^..//' | cho)
git switch -q $(echo "$selected" | cut -d' ' -f1)
git s
